<style>
  html,
  body {
    height: 100%;
    margin: 0;
    padding: 0;
  }

  .wrapper {
    min-height: 95vh;
    display: flex;
    flex-direction: column;
  }

  footer {
    /* height: 7vh; */
    margin-top: auto;
  }
</style>
</div>
<footer class="mt-auto bg-dark text-white pt-4 pb-3">
  <div class="container">
    <div class="row text-center text-md-left">
      <!-- Name & Address -->
      <div class="col-md-4 mb-3">
        <h5 class="text-uppercase">Arpit Mittal</h5>
        <p>
          55W3+92X, Nimbalkar Ki Goth No.2,<br>
          Lohiya Bazaar, Gwalior,<br>
          Madhya Pradesh – 474001, India
        </p>
      </div>

      <!-- Contact Info -->
      <div class="col-md-4 mb-3">
        <h5 class="text-uppercase">Contact</h5>
        <p>Email: <a href="mailto:arpitmittal597@gmail.com" class="text-white text-decoration-none">arpitmittal597@gmail.com</a></p>
        <p>Phone: <a href="tel:+919425785002" class="text-white text-decoration-none">+91 94257 85002</a></p>
      </div>

      <!-- Social Links -->
      <div class="col-md-4 mb-3">
        <h5 class="text-uppercase">Follow Me</h5>
        <a href="https://www.instagram.com/arpit_597/" class="text-white me-3"><i class="fab fa-instagram"></i></a>
        <a href="https://www.linkedin.com/in/arpit-597-mittal/" target="_blank" class="text-white me-3"><i class="fab fa-linkedin-in"></i></a>
        <a href="https://github.com/Arpit-597" target="_blank" class="text-white"><i class="fab fa-github"></i></a>
      </div>
    </div>

    <hr class="bg-white">

    <!-- Copyright -->
    <div class="text-center small">
      &copy; <%= new Date().getFullYear() %> Arpit Mittal. All rights reserved. | Designed & Developed by Arpit Mittal
    </div>
  </div>
</footer>


<!-- Bootstrap core JavaScript
    ================================================== -->
<!-- Placed at the end of the document so the pages load faster -->
<script src="https://code.jquery.com/jquery-3.3.1.slim.min.js"></script>
<script src="/assets/js/bootstrap.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.7/dist/js/bootstrap.bundle.min.js" integrity="sha384-ndDqU0Gzau9qJ1lfW4pNLlhNTkCfHzAVBReH9diLvGRem5+R9g2FzA8ZGN954O5Q" crossorigin="anonymous"></script>
<script>
  window.addEventListener('scroll', function() {
    const navbar = document.getElementById('mainNavbar');
    if (window.scrollY > 50) {
      navbar.classList.add('scrolled');
    } else {
      navbar.classList.remove('scrolled');
    }
  });
</script>
<script>
  let skip = 6;

  document.getElementById("loadMoreBtn").addEventListener("click", async () => {
    try {
      const res = await fetch(`/load-more-blogs?skip=${skip}`);
      const data = await res.json();
      skip += data.blogs.length;

      if (data.blogs.length === 0) {
        document.getElementById("loadMoreBtn").style.display = "none";
        return;
      }

      const container = document.getElementById("blog-container");
      data.blogs.forEach(blog => {
        const col = document.createElement("div");
        col.className = "col-md-4 blog-post";

        col.innerHTML = `
          <div class="blog-box">
            <img src="${blog.image.url}" alt="blog" width="316" height="210" />
            <h3>${blog.title}</h3>
            <p>by <span>Arpit Mittal</span> on <span>${new Date(blog.createdAt).toLocaleDateString('en-US', {
              year: 'numeric', month: 'long', day: 'numeric'
            })}</span></p>
            <p>${blog.description.slice(0, 100)}</p>
            <a href="/blog-details/${blog._id}" class="btn btn-danger">Read more...</a>
          </div>
        `;
        container.appendChild(col);
      });
    } catch (err) {
      console.error(err);
    }
  });
</script>



</body>

</html>